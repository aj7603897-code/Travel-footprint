# 旅行足迹

一个纯前端个人旅行记录 Web 应用，用中国地图点亮去过的城市/地区，并支持游记、照片、心愿计划和本地数据持久化。全国图点击省份后会切换为全国市级地图，当前省正常显示，其他省暗化；已去和心愿状态会直接填色到对应地图区域。

## 使用方式

在项目目录启动一个静态服务器：

```bash
python3 -m http.server 4173
```

然后访问：

```text
http://localhost:4173
```

数据保存在浏览器 `localStorage` 中，可在页面右上角导出或导入 JSON 备份。
